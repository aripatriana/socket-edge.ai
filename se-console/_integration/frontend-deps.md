# Frontend dependencies for Chat 3a

Run in `src/main/webapp-ui/`:

```
npm install @tanstack/react-query recharts
```

Results in `package.json`:

```
"@tanstack/react-query": "^5.59.0",
"recharts": "^2.13.0"
```
