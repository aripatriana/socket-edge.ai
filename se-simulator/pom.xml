<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.socket.edge</groupId>
        <artifactId>socket-edge-ai</artifactId>
        <version>4.0.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <groupId>com.socket.edge</groupId>
    <artifactId>se-simulator</artifactId>
    <version>4.0.0</version>
    <name>SE-simulator</name>
    <packaging>jar</packaging>
    <description>Simple Netty client/server simulator for integration testing</description>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <java.version>21</java.version>
        <netty.version>4.1.112.Final</netty.version>
    </properties>

    <dependencies>
        <!-- se-core: provides ByteDecoder, ByteEncoder, and Netty transitively -->
        <dependency>
            <groupId>com.socket.edge</groupId>
            <artifactId>se-core</artifactId>
            <version>4.0.0</version>
        </dependency>
    </dependencies>

    <build>
        <finalName>${project.artifactId}-${project.version}</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>${java.version}</release>
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
